---
// functions
import { timeAgo } from "@/functions/DateTime"

// props
export interface Props {
    post: object
}
const { post } = Astro.props;
---

<a class="space-y-4 group" href={post.url} title={post.title}>
    <div class="space-y-2">
        <h3 class="h5 group-hover:underline w-fit">{post.title}</h3>
        
        <div class="flex gap-2 text-stone-500 font-light">
            <time>{timeAgo(post.date)}</time>
            <span>{post.category}</span>
        </div>
    </div>
    
    <p class="font-light">{post.description}</p>
</a>