---
import Search from '@/components/Search'
---

<div class="sidebar">
    <slot />
</div>

<style lang="scss">
    @use '../assets/styles/breakpoints' as *;
    
    .sidebar {
        position: sticky;
        display: flex;
        flex-direction: column;
        gap: var(--space-xl);
        top: var(--space-lg);
        align-self: start;
    }
</style>