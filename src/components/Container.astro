---
// props
export interface Props {
    classes?: string
}
const { classes = "space-y-24" } = Astro.props;
---

<div class={`px-4 sm:px-8 lg:px-16 max-w-screen-2xl container mx-auto ${classes}`}>
    <slot />
</div>