---
// props
export interface Props {
    tags: object,
    base: string
}
const { tags, base } = Astro.props;
---

<ul class="tag-list">
    {tags.map(t =>
        <li class="tag">
            <a href={`/${base}/tags/${t}`} title={`Posts tagged ${t}`}>{t}</a>
        </li>
    )}
</ul>