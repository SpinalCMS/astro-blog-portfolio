---
// components
import Container from "@/components/Container"

// data
import Navigation from "@/data/navigation"
---

<Container>
    <header class="flex flex-row py-8 mt-24 justify-between items-center">
        <div class="flex flex-row items-center gap-x-8">
            <!-- logo -->
            <div>
                <a class="font-extrabold tracking-tighter text-3xl lowercase" href="/" title="Homepage">
                    ErikOlsen
                </a>
            </div>

            <!-- navigation items -->
            <nav class="flex flex-row gap-x-6">
                {Navigation.map((item) => (
                    <a class="text-stone-800 lowercase" href={item.url} title={item.display}>
                        {item.display}
                    </a>
                ))}
            </nav>
        </div>

        <a href="/contact" class="btn-primary">
            Contact Me
        </a>
    </header>
</Container>